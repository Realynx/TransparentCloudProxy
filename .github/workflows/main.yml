name: Run C# BuildDef
runs-on: self-hosted

on:
  push:
    branches:
      - '*'
  workflow_dispatch:

jobs:
  build:
    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Run BuildDef.cs
      run: dotnet run BuildDef.cs
